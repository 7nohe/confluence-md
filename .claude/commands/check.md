---
description: Run typecheck, tests, and build to verify everything works
---

Full verification of the project:

1. Run `npm run typecheck` - verify TypeScript types
2. Run `npm test` - run all tests
3. Run `npm run build` - build with ncc

Report any errors and suggest fixes. Stop at first failure.
